{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "studyground",
  "version": "0.1.0",
  "description": "Learn by reading rendered lessons in a local web reader, with Claude Code as the tutor and VSCode for hands-on coding.",
  "author": { "name": "studyground" },
  "license": "MIT",
  "keywords": ["learning", "study", "education", "ai", "markdown", "katex"],
  "userConfig": {
    "studyground_dir": {
      "type": "directory",
      "title": "Studyground directory",
      "description": "Where lessons, exercises, and progress are stored.",
      "default": "~/studyground"
    },
    "web_port": {
      "type": "number",
      "title": "Local web port",
      "description": "Port for the local reader UI.",
      "default": 4321
    },
    "chunk_chars": {
      "type": "number",
      "title": "Chunk size (chars)",
      "description": "Approximate character count per RAG chunk when processing uploaded materials.",
      "default": 1200
    },
    "chunk_overlap": {
      "type": "number",
      "title": "Chunk overlap (chars)",
      "description": "Characters of overlap between adjacent chunks.",
      "default": 200
    },
    "embeddings_provider": {
      "type": "string",
      "title": "Embeddings provider",
      "description": "Vector embedding backend: 'auto' uses whichever API key is set (VOYAGE_API_KEY > OPENAI_API_KEY); 'off' disables vectors.",
      "default": "auto"
    },
    "hybrid_alpha": {
      "type": "number",
      "title": "Hybrid retrieval α",
      "description": "Weight for BM25 in hybrid scoring: score = α·bm25 + (1−α)·cosine (default 0.4).",
      "default": 0.4
    }
  }
}
